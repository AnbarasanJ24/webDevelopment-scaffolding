/*Store Module for feature will be declared using key(any) and feature reducer
It will support single or multiple reducers */

/*@NgModel({
    import : [
        StoreModule.forfeature('featureName', featureReducer) -> Single Reducer

        StoreModule.forfeature('featureName', {
            subfeature1 : subfeature1Reducer1,
            subfeature2 : subfeature1Reducer2
        }) -> Multiple Reducer

    ],
});*/